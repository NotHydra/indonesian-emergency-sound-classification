```bash
sudo docker compose --profile development up --build #optional
sudo docker compose --profile development up

# or

sudo docker compose --profile production up --build #optional
sudo docker compose --profile production up
```
